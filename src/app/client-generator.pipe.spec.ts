import { ClientGeneratorPipe } from './client-generator.pipe';

describe('ClientGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
